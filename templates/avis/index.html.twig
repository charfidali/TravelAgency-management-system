{% extends 'base.back.html.twig' %}

{% block title %}Commentaires{% endblock %}

{% block body %}
<section id="main-content" style="background-color:#EDFFCF;">
<section class="wrapper site-min-height">
<div style="text-align :center;">
<h3 style="color :black; font-family: Georgia, serif;" >Liste de toute les avis</h3>
</div>
        <div class="row">
          <div class="col-lg-12">

{% for c in avis %}

            <div class="col-lg-4 col-md-4 col-sm-4 col-xs-12">
              <div class="custom-box">
                
                <ul class="pricing">
               <li>utilisateur :  {{ c.user.username }}</li>
                <li>a rater : {{ c.hotel.nom}}</li>
                <li>rate: {{ c.rate }} étoiles</li>
                
                </ul>
                                <ul class="pricing">
                </ul>
                <form method="post" action="{{ path( 'deleteavis' , {'id':c.id} )}}" style="display: inline-block;">
                <input type="hidden" name="_method" value="DELETE">
                <button class="btn btn-danger">Supprimer</button>
                </form>
                
              </div>
              <!-- end custombox -->
            </div>
            {% endfor %}
            <!-- end col-4 -->
  
            <!-- end col-4 -->
          </div>
          <!--  /col-lg-12 -->
        </div>
        <!--  /row -->
      </section>
      </section>
{% endblock %}
